// Les métadonnées du quiz (titre, description, canonical localisé, hreflang) sont
// désormais gérées par la page (generateMetadata), source unique de vérité.
export default function QuizLayout({ children }: { children: React.ReactNode }) {
  return children;
}
